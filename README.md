🚀 Estudo de API: Autenticação e Integração com SwaggerEste projeto foi desenvolvido como um exercício prático para compreender como o Swagger (OpenAPI 3.0) funciona como uma ferramenta de documentação e como integrar essas especificações em um backend real utilizando Java e Spring Boot.O foco principal é a simulação de um sistema de gerenciamento de contas e autenticação de usuários.📌 Objetivos do ProjetoDocumentação Primeiro (API First): Criação de contratos YAML para definir endpoints antes da implementação.Simulação de Login: Implementação de um fluxo de autenticação via POST retornando tokens JWT fictícios.Gestão de Contas: Endpoints para criação (POST), listagem (GET) e busca por ID (GET /accounts/{id}).Tratamento de Erros: Configuração de respostas HTTP apropriadas (200, 201, 400, 401, 404, 500).🛠️ Tecnologias UtilizadasJava 17+Spring Boot 3.x (Spring Web)SpringDoc OpenAPI (Para gerar o Swagger UI localmente)Maven (Gerenciador de dependências)IntelliJ IDEA Community Edition📂 Estrutura de EndpointsMétodoEndpointDescriçãoRequisitoPOST/loginRealiza autenticaçãoJSON (email/password)GET/accountsLista todas as contasToken no HeaderPOST/accountsCria uma nova contaToken no Header + JSONGET/accounts/{id}Busca conta por IDToken no Header + ID na URL🚀 Como Executar o ProjetoClone o repositório:Bashgit clone https://https://github.com/MarciaMagaX/swagger_api_basico.git
Abra no IntelliJ:Vá em File > Open e selecione a pasta do projeto.Aguarde o Maven baixar as dependências.Execute a aplicação:Localize a classe com a anotação @SpringBootApplication.Clique no botão Run (Play verde).Acesse a Documentação:Com o projeto rodando, abra o navegador em: http://localhost:8080/swagger-ui/index.htmlShutterstock🛡️ Segurança e Boas PráticasProteção de Dados: O sistema foi configurado para não retornar senhas nos corpos das respostas (body_response), seguindo recomendações de segurança da OWASP.Headers: Uso de headers customizados para validação de tokens JWT.⭐ Projeto desenvolvido para fins didáticos por Marcia Xavier.
